public class sameParity3875 {

  public boolean uniformArray(int[] nums1) {
    //since i can choose any index into the array, that's why it is always possible. if element is even i will choose an odd number ( for i!=j) and vice-versa bcz(even -odd = odd).
    return true;
  }
}
